node_modules/
dist/
dist-electron/
dist-server/
release/
*.log
.DS_Store

# Local runtime data (PGlite server cluster) — never commit
opennote-sync-data/

# Local agent tooling config (machine-specific absolute paths)
.codex/
.gstack/
